{
  "scripts": {
    "lint:fix": "prettier */*.js \"*/**/*{.js,.ts}\" -w",
    "lint": "prettier */*.js \"*/**/*{.js,.ts}\" --check",
    "test": "npx amman run -- anchor test --skip-local-validator",
    "solita": "solita",
    "airdrop": "amman airdrop wallet.json 100",
    "dev:app": "pnpm --filter fragmentor-app run dev",
    "compile": "pnpm --filter fragmentor-sdk run compile",
    "vitest": "pnpm --filter fragmentor-sdk run vitest"
  },
  "dependencies": {
    "@metaplex-foundation/amman": "0.12.1",
    "@metaplex-foundation/amman-client": "0.2.4"
  },
  "devDependencies": {
    "@metaplex-foundation/solita": "0.20.1",
    "prettier": "3.1.0",
    "typescript": "5.3.2"
  },
  "private": true
}
